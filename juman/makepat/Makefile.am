pkglibexecdir = $(libexecdir)/juman
pkglibexec_PROGRAMS = makepat dicsort

INCLUDES = -I../lib
LDADD = $(top_builddir)/lib/libjuman.la -lstdc++

makepat_SOURCES = makepat.c
dicsort_SOURCES = dicsort.c
